# CLAUDE.md

本文件用于约束 Claude Code 在本仓库中的工作方式。请优先遵守本文件，再结合用户的具体指令执行。

## 项目概述

这是一个公务员考试备考智能复盘与番茄钟工具，主体是一个可直接运行的单文件前端应用。

- 主入口：[index.html](index.html)
- 技术栈：原生 HTML / CSS / JavaScript
- 数据存储：浏览器 `localStorage`
- 图表：Chart.js CDN
- 无后端、无构建步骤、无包管理依赖

## 核心原则

1. **保持单文件应用**
   - 除非用户明确要求，不要拆分为多文件框架项目。
   - 不要引入 React、Vue、Vite、Webpack、Node 服务端或数据库。

2. **保持可直接运行**
   - 修改后应仍可通过浏览器直接打开 [index.html](index.html) 运行。
   - 不要加入需要安装依赖或构建后才能运行的代码。

3. **本地优先**
   - 业务数据继续保存在 `localStorage`。
   - 不要擅自增加云同步、登录、后端 API 或远程数据库。

4. **小步、精准修改**
   - 只修改与用户请求直接相关的代码。
   - 不做无关重构。
   - 保持现有代码风格、命名和注释密度。

## 功能边界

当前应用应持续支持：

- 仪表盘统计
- 番茄钟倒计时与正向计时
- 提前结束并记录学习时长
- 学习时长按时、分、秒展示
- 计划管理
- 错题管理
- 每日 / 每周复盘
- 科目管理
- OpenAI / DeepSeek 兼容的 AI 接口配置
- 所有业务数据本地持久化

## 数据约定

localStorage 主键：

```js
civilServiceExamReviewPomodoroStateV1
```

学习记录应优先使用秒级字段：

```js
durationSeconds
```

为兼容旧数据，可以继续读取旧字段：

```js
minutes
```

但新增记录应写入 `durationSeconds`，避免未满一分钟被错误算作一分钟。

## UI 风格

整体视觉风格应保持：

- 冷色系
- 浅蓝 / 淡紫 / 青蓝渐变背景
- 玻璃拟态卡片
- 圆角、柔和阴影
- 简洁、清爽、低干扰

番茄钟区域桌面端应优先保持：

- 左侧时钟
- 右侧设置
- 横向铺满主内容区域
- 小屏幕自动上下排列

## AI 设置说明

用户明确要求 API Key、Base URL、模型名保存在浏览器本地。

因此可以继续使用 `localStorage` 保存 AI 配置，但不要声称它是强安全存储。涉及安全说明时，应明确：

- 仅适合个人本地使用
- 浏览器本地存储不是加密保险箱
- 不应在不可信设备上保存敏感 Key

## 验证要求

修改 [index.html](index.html) 后，优先做以下检查：

1. JavaScript 语法检查
   - 提取内联 `<script>` 内容并用 Node `vm.Script` 验证。
2. 浏览器手动检查
   - 页面能打开。
   - 控制台无新增运行时错误。
   - localStorage 数据能正常读写。
3. 与修改相关的功能检查
   - 如果改番茄钟，检查倒计时、正向计时、暂停、重置、结束并记录。
   - 如果改仪表盘，检查统计和图表刷新。
   - 如果改表单，检查新增、编辑、删除、保存。

如果工具环境无法运行 Bash，应如实说明，并给出用户可手动运行的命令。

## Git 工作流

- 不要自动提交或推送，除非用户明确要求。
- 修改前可查看 `git status`。
- 完成后报告改动文件和验证结果。
- 不要删除用户未授权删除的文件。

## 禁止事项

除非用户明确要求，不要：

- 引入构建工具或依赖管理。
- 改成多页面应用或框架应用。
- 添加后端服务。
- 更换存储方案。
- 删除现有业务功能。
- 大规模重排无关代码。
- 把 API Key 写入源码或文档示例中的真实值。
